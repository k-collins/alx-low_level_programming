#include "main.h"
#include <stdio.h>

/**
 * mul - a function that  multiply two integers
 *
 * @a: The parameter a
 * @b: The parameter b
 *
 * Return: Returns 1 is uppercase, 0 if otherwise
 *
 */

int mul(int a, int b)
{
	return (a * b);
}
