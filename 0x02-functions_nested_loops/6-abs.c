#include "main.h"

/**
 * _abs - function for absolute
 *
 * @number: the number to be checked
 *
 * Return: to return the value
 *
 */

int _abs(int number)
{
	if (number < 0)
	{
		return (-number);
	}
	else
	{
		return (number);
	}
}
